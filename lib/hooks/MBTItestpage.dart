import 'package:flutter/material.dart';

class MBTItestpage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('MBTI test'),
      ),
      body: Center(
        child: Text('MBTI test Page'),
      ),
    );
  }
}
